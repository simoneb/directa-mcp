[build-system]
# 77 or newer for PEP 639 licence metadata (the `license` SPDX expression and
# `license-files`), which older setuptools ignores or rejects.
requires = ["setuptools>=77", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "directa-mcp"
version = "0.3.0"
description = "MCP server exposing Directa SIM's Darwin API (dAPI) as tools for any MCP client"
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
license-files = ["LICENSE"]
authors = [
  { name = "Simone Busoli" }
]
keywords = ["mcp", "directa", "darwin", "dapi", "trading", "borsa-italiana"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Environment :: Console",
  "Intended Audience :: Financial and Insurance Industry",
  "Operating System :: OS Independent",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Topic :: Office/Business :: Financial :: Investment",
]
dependencies = [
  "mcp>=1.6.0,<2.0",
  "python-dotenv>=1.0,<2.0",
]

[project.urls]
Repository = "https://github.com/simoneb/directa-mcp"
"Protocol notes" = "https://github.com/simoneb/directa-mcp/blob/master/docs/PROTOCOL.md"

[project.optional-dependencies]
dev = ["pytest>=8.0,<9.0"]

[project.scripts]
directa-mcp = "directa_mcp.server:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
